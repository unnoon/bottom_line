{
  "name": "bottom_line",
  "description": "Modern JS utility functions",
  "version": "0.7.0",
  "homepage": "https://github.com/unnoon/bottom_line",
  "author": {
    "name": "Rogier Geertzema",
    "email": "r.geertzema@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/unnoon/bottom_line"
  },
  "license": "MIT",
  "keywords": [
    "tools",
    "utilities",
    "toolbelt",
    "utilitybelt",
    "typescript",
    "decorators",
    "generators"
  ],
  "scripts": {
    "build:clean": "rimraf -rf bin/ && rimraf -rf tmp/",
    "build:es6": "tsc --outDir tmp/ -p tsconfig.json --declaration --sourceMap --module ES6 && ncp tmp/src/ bin/",
    "build:ts": "ncp src/ bin/.ts/",
    "build": "npm run build:clean && npm run build:es6 && npm run build:ts && ncp package.json bin/package.json",
    "docs": "rimraf -rf doc && typedoc --options .typedoc.json --out doc/ src/",
    "dts": "dts-generator --name bottom_line --project ./ --out index.d.ts",
    "release": "npm version",
    "prepush": "npm test",
    "preversion": "npm test && npm run docs",
    "version": "npm run build && git add -A bin",
    "postversion": "git push && git push --tags && npm publish bin/",
    "test": "karma start --single-run",
    "update": "ncu -u -a && npm install"
  },
  "dependencies": {
    "@types/lodash": "^4.14.74",
    "@types/reflect-metadata": "^0.0.5",
    "core-js": "^2.5.1",
    "lodash": "^4.17.4",
    "lodash-decorators": "^4.4.1",
    "reflect-metadata": "^0.1.10"
  },
  "devDependencies": {
    "@types/chai": "^4.0.4",
    "@types/mocha": "^2.2.42",
    "@types/sinon": "^2.3.3",
    "awesome-typescript-loader": "^3.2.3",
    "chai": "^4.1.2",
    "coveralls": "^2.13.1",
    "deep-eql": "3.0.0",
    "husky": "^0.14.3",
    "karma": "^1.7.1",
    "karma-chai": "^0.1.0",
    "karma-chrome-launcher": "^2.2.0",
    "karma-firefox-launcher": "^1.0.1",
    "karma-mocha": "^1.3.0",
    "karma-sinon": "^1.0.5",
    "karma-typescript": "^3.0.5",
    "metascript": "^1.0.0",
    "mkdirp": "0.5.1",
    "mocha": "^3.5.0",
    "rimraf": "^2.6.1",
    "sinon": "^3.2.1",
    "tslint": "^5.7.0",
    "tslint-eslint-rules": "^4.1.1",
    "typescript": "^2.5.2",
    "webpack": "^3.5.5"
  }
}
